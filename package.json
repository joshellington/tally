{
  "name": "tally",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "offline": "nodemon --exec serverless offline --host 0.0.0.0 start -e js",
    "offline:debug": "nodemon --exec SLS_DEBUG=* serverless offline --host 0.0.0.0 start -e js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.828.0",
    "md5": "^2.3.0",
    "serverless": "^2.19.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "serverless-dotenv-plugin": "^3.1.0",
    "serverless-offline": "^6.8.0"
  }
}
